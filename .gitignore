# ⚠️ 运行数据 —— 绝对不要提交。
# data/config.yaml 里有：访问密码的 scrypt 哈希、会话签名密钥 secret_key、
# 以及密码传输用的 RSA 私钥 transport_key。提交上去等于把这台机器的门钥匙
# 连同你的内网拓扑一起公开。
data/
config.yaml
*.yaml.bak
*.yaml.tmp
*.pre-merge
*.uploaded

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# 编辑器 / 系统
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
