# 服务索引 运行依赖
# 已在 Flask 3.1.3 / Werkzeug 3.1.7 / PyYAML 6.0.3 / gunicorn 23.x 下测试通过。
# 这里按大版本约束，保证长期可复现安装。

Flask>=3.0,<4.0
Werkzeug>=3.0,<4.0      # 直接用到 werkzeug.security 做密码哈希
cryptography>=42.0      # RSA-OAEP 解密前端加密提交的密码
PyYAML>=6.0,<7.0
gunicorn>=22.0,<24.0    # 生产环境 WSGI 服务器
